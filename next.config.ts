import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com', 'avatars.githubusercontent.com', 'source.unsplash.com', 'media2.dev.to'],
  },
};

module.exports = (nextConfig);